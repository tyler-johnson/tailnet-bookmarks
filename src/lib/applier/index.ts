// Module surface for the `applier` component (DESIGN.md "Components").
// Consumed by the background run loop (src/lib/background/run.ts).

export { applyOps } from './applier';
export type { ApplierContext, ApplyResult, BookmarksAPI } from './applier';
